in:
  type: file
  path_prefix: {{ env.CD_FETCHED }}/learning_outcome_rubric_criterion_fact
  decoders:
  - {type: gzip}
  parser:
    charset: UTF-8
    newline: LF
    type: csv
    delimiter: "\t"
    quote: null
    quotes_in_quoted_fields: ACCEPT_STRAY_QUOTES_ASSUMING_NO_DELIMITERS_IN_FIELDS
    escape: '"'
    null_string: \N
    trim_if_not_quoted: false
    skip_header_lines: 0
    allow_extra_columns: false
    allow_optional_columns: false
    columns:
    - {name: learning_outcome_rubric_criterion_id, type: long}
    - {name: learning_outcome_id, type: long}
    - {name: account_id, type: long}
    - {name: course_id, type: long}
    - {name: enrollment_term_id, type: long}
    - {name: points, type: double}
out:
  type: sqlserver
  driver_path: {{ env.EMBULK_MSSQL_DRIVER }}
  native_driver: {{ env.EMBULK_NATIVE_DRIVER }}
  host: {{ env.EMBULK_MSSQLH }}
  port: {{ env.EMBULK_MSSQLHP }}
  user: {{ env.EMBULK_MSSQLU }}
  password: {{ env.EMBULK_MSSQLP }}
  database: {{ env.EMBULK_MSSQLDB }}
  default_timezone: {{ env.EMBULK_TZ }}
  table: learning_outcome_rubric_criterion_fact
  mode: replace
  insert_method: native
  column_options:
    learning_outcome_rubric_criterion_id: {type: 'BIGINT NOT NULL PRIMARY KEY'}
    learning_outcome_id: {type: 'BIGINT NULL'}
    account_id: {type: 'BIGINT NULL'}
    course_id: {type: 'BIGINT NULL'}
    enrollment_term_id: {type: 'BIGINT NULL'}
    points: {type: 'FLOAT NULL'}
  after_load: |
    CREATE INDEX [learning_outcome_id] ON [dbo].[learning_outcome_rubric_criterion_fact] ([learning_outcome_id] ASC);
    CREATE INDEX [account_id] ON [dbo].[learning_outcome_rubric_criterion_fact] ([account_id] ASC);
    CREATE INDEX [course_id] ON [dbo].[learning_outcome_rubric_criterion_fact] ([course_id] ASC);
    CREATE INDEX [enrollment_term_id] ON [dbo].[learning_outcome_rubric_criterion_fact] ([enrollment_term_id] ASC);